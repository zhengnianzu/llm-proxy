# OBS 同步配置
# 用法: sync config settings/obs_base.yaml

# OBS base 路径（必填）
obs_base: obs://s3-asset-b-hd-cce-aifm-nlp-exp/raw/proxy/

# 同步间隔（秒），默认 600（10分钟）
interval: 600

# 并发上传线程数，默认 4
workers: 20

# 自定义上传脚本路径（可选），默认使用 obs_utils
# upload_script: /path/to/upload.sh
